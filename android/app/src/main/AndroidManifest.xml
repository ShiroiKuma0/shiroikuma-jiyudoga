<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
  <!-- The automation data door runs its export/import in a `specialUse` foreground service. At
       targetSdk 34 the foregroundServiceType alone is not enough: without this the service throws
       at startForeground. -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>

  <!-- Optional: lets the 保存復元 automation contract honour an absolute `path` extra
       (e.g. the shared 「[979] バックアップ」 folder). Without the grant the export falls
       back to the SAF folder picked in the Export/Import panel. -->
  <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
    tools:ignore="ScopedStorage" />

  <!-- package visibility (Android 11+) for the shiroikuma-jisho study hand-off and for
       replying to 自由作業盤's token-gated backup broadcasts -->
  <queries>
    <package android:name="shiroikuma.jisho" />
    <package android:name="shiroikuma.jiyusagyoban" />
    <!-- 応用管理 drives the contract v2 restore. BOTH callers must be named: without the entry a
         reply broadcast's setPackage fails SILENTLY on Android 11+ — the export runs, writes
         correctly, and is never heard of. -->
    <package android:name="shiroikuma.oyokanri" />
  </queries>

  <supports-screens
    android:anyDensity="true"
    android:smallScreens="true"
    android:normalScreens="true"
    android:largeScreens="true"
    android:resizeable="true"
    android:xlargeScreens="true" />

  <application
    android:allowBackup="true"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:fullBackupContent="@xml/backup_rules"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.FreeTubeAndroid"
    android:hardwareAccelerated="true"
    android:grantUriPermissions="true"
    tools:targetApi="34">

    <service
        android:name=".KeepAliveService"
        android:foregroundServiceType="mediaPlayback"
        android:exported="false">
    </service>

    <!-- 保存復元 automation contract: 自由作業盤 backs up every sister app in one batch.
         No android:permission — the caller cannot hold one; the token is the gate. -->
    <receiver
      android:name=".backup.StateExportReceiver"
      android:exported="true">
      <intent-filter>
        <action android:name="${applicationId}.action.EXPORT_STATE" />
        <action android:name="${applicationId}.action.LIST_CATEGORIES" />
        <action android:name="${applicationId}.action.CANCEL_EXPORT" />
      </intent-filter>
    </receiver>

    <receiver android:name=".MediaControlsReceiver"
      android:exported="true">
      <intent-filter>
        <action android:name="play" />
        <action android:name="pause" />
        <action android:name="previous" />
        <action android:name="next" />
      </intent-filter>
    </receiver>

    <activity
      android:name=".MainActivity"
      android:exported="true"
      android:label="@string/app_name"
      android:theme="@style/Theme.FreeTubeAndroid"
      android:configChanges="screenSize|smallestScreenSize|orientation|screenLayout|uiMode"
      android:launchMode="singleTask">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />

      </intent-filter>

      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />

        <data android:scheme="https" />
        <data android:host="m.youtube.com" />
        <data android:host="www.youtube.com" />
        <data android:host="youtube.com" />
        <data android:host="youtu.be" />
        <!-- supported links -->
          <data android:host="invidious.fdn.fr" />
          <data android:host="vid.puffyan.us" />
          <data android:host="invidious.flokinet.to" />
          <data android:host="inv.bp.projectsegfau.lt" />
          <data android:host="invidious.lunar.icu" />
          <data android:host="invidious.io.lol" />
          <data android:host="inv.tux.pizza" />
          <data android:host="invidious.privacydev.net" />
          <data android:host="yt.artemislena.eu" />
          <data android:host="vid.priv.au" />
          <data android:host="onion.tube" />
          <data android:host="yt.oelrichsgarcia.de" />
          <data android:host="invidious.protokolla.fi" />
          <data android:host="invidious.asir.dev" />
          <data android:host="iv.nboeck.de" />
          <data android:host="invidious.private.coffee" />
          <data android:host="iv.datura.network" />
          <data android:host="anontube.lvkaszus.pl" />
          <data android:host="inv.us.projectsegfau.lt" />
          <data android:host="invidious.perennialte.ch" />
          <data android:host="invidious.drgns.space" />
          <data android:host="invidious.einfachzocken.eu" />
          <data android:host="invidious.slipfox.xyz" />
          <data android:host="invidious.no-logs.com" />
          <data android:host="yt.drgnz.club" />
          <data android:host="yt.cdaut.de" />
          <data android:host="iv.melmac.space" />
          <data android:host="inv.citw.lgbt" />
        <!-- /supported links -->
      </intent-filter>

    </activity>


    <!-- Contract v2 §2a: the data door. Exported with no permission, on purpose — a custom
         permission would have to be `normal` (any app may hold it) or `signature` (impossible:
         every app in this family has its own keystore), and neither buys anything the caller
         check does not. AutomationCallers verifies the caller by exact package name, uid, and a
         pinned signing certificate before anything else happens. -->
    <provider
      android:name=".automation.AutomationProvider"
      android:authorities="${applicationId}.automation"
      android:exported="true" />

    <service
      android:name=".automation.AutomationDataService"
      android:exported="false"
      android:foregroundServiceType="specialUse">
      <property
        android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
        android:value="Automation data export/import" />
    </service>

    <!-- Capability discovery. Read with MATCH_UNINSTALLED_PACKAGES | MATCH_DISABLED_COMPONENTS,
         because a frozen app cannot be asked anything and 白い熊 freezes aggressively. An app
         without these is simply not offered for data backup — that is the opt-out. -->
    <meta-data android:name="shiroikuma.automation.contract" android:value="2" />
    <meta-data android:name="shiroikuma.automation.format" android:value="1" />
    <meta-data android:name="shiroikuma.automation.min_format" android:value="1" />

    <!-- Serves the console log to the system share sheet. Not exported: the receiving app is
         granted the single URI the share intent carries, for as long as it holds it. -->
    <provider
      android:name="androidx.core.content.FileProvider"
      android:authorities="${applicationId}.fileprovider"
      android:exported="false"
      android:grantUriPermissions="true">
      <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/file_paths" />
    </provider>

  </application>

</manifest>
